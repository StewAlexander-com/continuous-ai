# Seedling Runtime — Python dependencies
# Pin versions for reproducibility. Update intentionally after testing.

ollama>=0.3.0
lancedb>=0.6.0
pyarrow>=15.0.0
pyyaml>=6.0
httpx>=0.27.0        # Perplexity API calls in critic.py
pymupdf>=1.24.0      # :read PDF text extraction (AGPL; user-directed only)

# Optional for scanned PDF OCR (also needs the tesseract system binary):
#   pip install pytesseract pillow
#   brew install tesseract   # macOS
#   apt install tesseract-ocr  # Debian/Ubuntu

# MLX (Apple Silicon only — required for RDST Phase 5)
# Install separately to avoid non-Apple platform errors:
#   pip install mlx-lm
# Then convert your model before tuning:
#   python -m mlx_lm.convert --hf-path <huggingface_model_id> --mlx-path ./models/<name>

# Optional: for development
# pytest>=8.0
# black>=24.0
